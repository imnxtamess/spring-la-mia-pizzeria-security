spring.application.name=spring-la-mia-pizzeria-crud

spring.datasource.url=jdbc:mysql://localhost:3306/pizzeria
spring.datasource.username=root
spring.datasource.password=spibbel1998

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Hibernate ddl auto(create, create-drop, validate, update, none)
spring.jpa.hibernate.ddl-auto=update

#Date formatting
spring.mvc.format.date=yyyy-MM-dd
spring.mvc.format.date-time=yyyy-MM-dd HH:mm:ss
spring.mvc.format.time=HH:mm:ss

#additional infos useful for testing(must be removed after)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
